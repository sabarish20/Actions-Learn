name: First-CI

on: [push, fork]

jobs: 
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome
              run: echo "Hello"

            - name: List Files
              run: ls
        
            - name: Read Files
              run: cat README.md