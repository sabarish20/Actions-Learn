name: First-CI

on: push

jobs: 
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Welcome
              run: echo "Hello"

            - name: List Files
              run: ls
        
            - name: Read Files
              run: cat README.md